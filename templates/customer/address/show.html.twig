{% extends 'base.html.twig' %}

{% block title %}Address{% endblock %}

{% block body %}
    <h1>Address</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ address.id }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ address.country }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ address.region }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ address.city }}</td>
            </tr>
            <tr>
                <th>Postcode</th>
                <td>{{ address.postcode }}</td>
            </tr>
            <tr>
                <th>Street</th>
                <td>{{ address.street }}</td>
            </tr>
            <tr>
                <th>Additional</th>
                <td>{{ address.additional }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ address.location }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('customer_address_index') }}">back to list</a>

    <a href="{{ path('customer_address_edit', {'id': address.id}) }}">edit</a>

    {{ include('customer/address/_delete_form.html.twig') }}
{% endblock %}
