<form method="post" action="{{ path('customer_request_active_delete', {'id': request_active.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ request_active.id) }}">
    <button class="btn">Delete</button>
</form>
