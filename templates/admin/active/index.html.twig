<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <link href='https://unpkg.com/boxicons@2.0.7/css/boxicons.min.css' rel='stylesheet'>
    <title>{% block title %}Demandes en cours{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>
{% block header %} {% include 'admin/layout/_header.html.twig' %} {% endblock %}

<section class="home-section">
    <nav>
        <div class="sidebar-button">
            {# <i class='bx bx-menu sidebarBtn'></i> #}
            <span class="dashboard"> </span>
        </div>

        {#<div class="profile-details">
            {<img src="images/profile.jpg" alt=""> #}
            <span class="admin_name">{{ app.user.email }}</span>{#
        </div>#}
    </nav>

    <div class="home-content">


        <div class="sales-boxes">
            <div class="recent-sales box">

            <h1>Liste des demandes en cours</h1>

    <table class="content-table">
        <thead>
            <tr>
                <th>Nom du client</th>
                <th>Service</th>
                <th>Nom du fixer</th>
                <th>Demande</th>

            </tr>
        </thead>
        <tbody>
        {% for request_active in request_actives %}
            <tr>
                <td>{{ request_active.request.customer.lastname }} {{ request_active.request.customer.lastname }}</td>
                <td>{{ request_active.request.service.name }}</td>
                <td>{{ request_active.fixer.alias}}</td>
                <td>
                    {{ request_active.request.subject }}<br>
                    {{ request_active.request.description }}<br>
                    {{ request_active.request.description }}<br>
                    Début le {{ request_active.createdAt ? request_active.createdAt|date('d-m-Y H:i:s') : '' }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

            </div>
        </div>
    </div>
</section>
</body>
</html>



